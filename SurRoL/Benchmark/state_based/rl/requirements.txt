# Core RL dependencies for SAC/DDPG/DEX training
# PyTorch - install manually based on your CUDA version:
# pip install torch==2.7.1+cu118 torchvision==0.22.1+cu118 torchaudio==2.7.1+cu118

# NumPy (CRITICAL: Must be <2.0 for PyBullet compatibility)
numpy<2

# Configuration management
hydra-core==1.3.2
omegaconf==2.3.0

# Experiment tracking (optional)
wandb>=0.9.1

# Utilities
packaging>=21.3
colorlog>=6.8.0
termcolor>=2.4.0

# Parallel training (optional - only if using multiple workers)
mpi4py>=3.1.5

# Memory monitoring
psutil